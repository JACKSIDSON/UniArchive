# 示例档案库

这是一个 UniArchive 示例档案库（Vault）。它不使用任何私有格式：

- `archives/` 下是普通 Markdown 文件（YAML frontmatter + 正文）
- `assets/` 下是附件原始文件
- `.uniarchive/` 下是配置与索引（索引可删除重建）

直接用 UniArchive 打开本文件夹即可浏览，也可以用任意文本编辑器阅读 `archives/` 下的内容。
